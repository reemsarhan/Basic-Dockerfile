# Use the lightweight Alpine Linux as the base image
FROM alpine:latest

# Use a build argument to pass a custom name, with a default value of "Captain"
ARG NAME=Captain

# Set the command to print "Hello, [NAME]!" and exit
CMD echo "Hello, $NAME!"

